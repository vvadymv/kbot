# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: ghcr.io/vvadymv
  # Overrides the image tag whose default is the chart appVersion.
  name: kbot
  tag: "v1.1.1-6c906e7"
  arch: amd64
  os: linux
secret:
  name: kbot
  env: TELE_TOKEN
  key: "7353443227:AAEY3azrVQEHRjnJlrHVqlbX2OvPvoahOMU"
serviceAccount:
  create: false
autoscaling:
  enabled: false
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: false
